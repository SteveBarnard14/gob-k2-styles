# Release Log

## Unreleased

### Ready For QA

| ID | Date | Area | Summary | Verification | Release Notes | Status |
| --- | --- | --- | --- | --- | --- | --- |
| GOB-K2-001 | 2026-07-29 | K2 Style Profile / MOB.Request.New.Form | Replaced the malformed wrapper with valid standalone CSS and added an interactive local preview of the New Mobile Device Request form. | Browser parsed 79 stylesheet rules with no console errors. Reviewed default, employee, and mobile-device states at 1280px, 390px, and 320px; no horizontal overflow was detected. | The preview recreates the K2 structure but does not execute K2 rules or submit data. Link the CSS last in Style Profile external files, disable the legacy theme, and use a cache-busting query string during deployment testing. | Ready for QA |
| GOB-K2-002 | 2026-07-29 | K2 runtime compatibility | Corrected sizing, conditional visibility, and long-text wrapping against the live SmartForm DOM. | Verified the K2 10px root, converted profile dimensions to explicit pixels, restored all `.hidden` controls, cleared inline section-cell backgrounds, and tested default, employee, and mobile-device branches at the intended 1024px form width. | K2 must use the cache-busted external-file URL for this revision. The CSS preserves K2 rule-driven visibility and does not change form business logic. | Ready for QA |
| GOB-K2-003 | 2026-07-29 | K2 visual parity | Matched the live MOB request form to the local reference with a compact branded header, white form body, consistent 44px controls, policy callout, and grouped actions. | Compared local and live views; verified a 118px live header against the 119px reference, a 74px inner header grid, 44px inputs and buttons, a 12px action gap, and no horizontal overflow in the default, employee, role, Mobile Phone, MiFi, or SIM Card states. | BahamasStyle version 3.0 uses the cache-busted stylesheet URL ending in `?v=47f0b07`. Authored copy, K2 grid row placement, default selections, and complete mobile reflow remain Designer-owned. | Ready for QA |
| GOB-K2-004 | 2026-07-29 | K2 editable list views | Added reusable styling for K2 list headers, toolbars, column headings, zebra rows, selected rows, inline editing, action focus, and responsive overflow while generalizing the shared form header. | Verified MOB.Request.Approval.Form with a 119px header, 52px toolbar, 44px column header, 50px rows, aqua selection state, four 44px edit inputs, primary Save state, and no horizontal overflow. Regression-tested MOB.Request.New.Form at a 119px header with no overflow. | BahamasStyle version 4.0 is checked in with the cache-busted URL ending in `?v=c58ee45`. Column definitions, list titles, paging availability, and data behavior remain controlled by K2 Designer. | Ready for QA |
| GOB-K2-005 | 2026-07-29 | K2 runtime shell | Increased the shared desktop content width from 1024px to 1280px while retaining responsive gutters and small-screen behavior. | At a 1711px viewport, verified both live forms at a 1280px runtime width with no horizontal overflow. The approval grid measured 1232px with approximately 298px columns and 284px inline-edit inputs; the request form measured three 394px authored columns. | BahamasStyle version 5.0 is checked in with the cache-busted URL ending in `?v=ec5044d`. The shell remains fluid below 1280px and preserves existing mobile breakpoints. | Ready for QA |
| GOB-K2-006 | 2026-07-29 | K2 dropdown controls | Vertically centered selected values and placeholders inside K2 dropdown boxes while preserving the dropdown button and neighboring composite controls. | On MOB.Request.New.Form, verified visible dropdown text changed from 2px top and 20px bottom spacing to an even 11px/11px. Confirmed 44px text inputs, unchanged calendar controls, and no horizontal overflow. | BahamasStyle version 6.0 is checked in with the cache-busted URL ending in `?v=265f4ff`. The rule targets `.select-box.dropdown-box` only and does not alter lookup pickers or calendars. | Ready for QA |
| GOB-K2-007 | 2026-07-29 | K2 control typography | Standardized data-entry text on a shared 16px token across text boxes, text areas, dropdowns, calendar and lookup inputs, radio options, and checkbox options. | On MOB.Request.New.Form, verified live computed sizes of 16px for every control family, including the conditional justification text area, using the same Segoe UI font stack. Confirmed no horizontal overflow at the 1711px test viewport. | BahamasStyle version 7.0 is checked in with the cache-busted URL ending in `?v=a2bfd3d`. Field labels, helper text, panel headings, and command buttons retain their intentional hierarchy. | Ready for QA |
| GOB-K2-008 | 2026-07-29 | K2 People Picker | Vertically centered single-line People Picker placeholders and entered values while applying the shared 16px control typography. | On the MOB.Request.New.Form employee branch, verified the picker changed from 2px top and 20px bottom spacing to 11px/11px for both placeholder and entered text. Regression-checked dropdown alignment, calendar and text-input heights, and horizontal overflow. | BahamasStyle version 8.0 is checked in with the cache-busted URL ending in `?v=ce4555a`. The rule uses the generic `.picker-input[data-maxrows="1"]` structure and requires no control or generated-cell name. | Ready for QA |
| GOB-K2-009 | 2026-07-29 | Standard K2 input compatibility | Extended the shared 16px data-entry typography to standard K2 native inputs, AutoComplete, Choice, List Box, Multi-Select, Picker, List Display, Tree, attachment text, and hyperlink values using runtime classes rather than Designer names. | Parsed 181 local stylesheet rules without error; mapped selectors against the installed K2 5.1017 runtime control assets. Verified 21 visible input surfaces on the New Request employee branch and four editable-list inputs on the Approval form at 16px, with picker and dropdown text at 11px/11px vertical spacing and no horizontal overflow. | BahamasStyle version 9.0 is checked in with the cache-busted URL ending in `?v=338284a`. Non-textual controls retain their component geometry. Rich Text iframe contents and arbitrary uploaded custom controls require separate integration because parent Style Profile CSS cannot guarantee their internal markup. | Ready for QA |
| GOB-K2-010 | 2026-07-29 | K2 form actions | Added balanced vertical padding around bottom form action buttons so they no longer touch the preceding view or policy section. | On MOB.Request.New.Form, verified the gap above Submit and Cancel changed from 0px to 24px, both action cells have 24px top and bottom padding, buttons remain 44px high, bottom clearance remains 64px, and no horizontal overflow occurs. Confirmed the Approval list toolbar has no matching footer-action cells and is unchanged. | BahamasStyle version 10.0 is checked in with the cache-busted URL ending in `?v=ef4112c`. The live rule targets right-aligned K2 Button cells structurally and does not depend on generated cell names. | Ready for QA |
| GOB-K2-011 | 2026-07-29 | Ministry of Finance naming compatibility | Added Ministry of Finance `MOF_*` selector aliases for shared form headers, notices, section headings, stacked choices, policy acknowledgement cells, action buttons, and editable-list Save state while retaining legacy MOB/NewDevice selectors during migration. | Parsed the stylesheet for balanced blocks, verified GitHub Pages serves the final CSS hash, confirmed `MOB.Request.New.Form` emits the renamed body-view controls after checking in `MOB.Request.ItemView` version 2.0, verified Submit and Cancel are right-aligned under the `MOF_*` names, and regression-checked `MOB.Request.Approval.Form` list width with no horizontal overflow. | BahamasStyle version 11.0 is checked in with the cache-busted URL ending in `?v=42b74e9`. Body-view style anchors were normalized in Designer where safely selectable: `MOF_StackedChoice`, `MOF_SectionHeading_WorkDetails`, `MOF_SectionHeading_DeviceDetails`, `MOF_HelpText`, `MOF_PolicyChoiceCell`, `MOF_PolicySpacerCell`, `MOF_PolicyBodyCell`, `MOF_PolicyBodyText`, `MOF_PrimaryActionCell`, `MOF_PrimaryAction`, `MOF_SecondaryActionCell`, and `MOF_SecondaryAction`. Header/form-shell names and two nested policy cells remain on compatibility aliases until they can be selected directly in Designer. | Ready for QA |
| GOB-K2-012 | 2026-07-30 | K2 mobile choice controls | Reworked mobile K2 radio and checkbox choice groups so table-cell option layouts collapse into full-width touch rows instead of clipping wrapped labels on iOS Chrome. | Reproduced the Device Type issue at a 390px viewport where K2 reduced each option text span to 7px and clipped the labels; parsed the updated stylesheet with balanced blocks; verified `MOB.Request.New.Form` loads `?v=6ad0bf1`, each Device Type option renders as a block row, option text spans measure about 138px instead of 7px, and horizontal overflow remains 0px at 390px. | BahamasStyle version 13.0 is checked in with the cache-busted URL ending in `?v=6ad0bf1`. The rule targets generic K2 choice controls by `[class*="ControlPack-Choice"]` at the mobile breakpoint, so it is not tied to the Device Type control name. | Ready for QA |
| GOB-K2-014 | 2026-07-30 | K2 runtime message boxes | Removed the nested inner panel frame from K2 popup message boxes and hid the runtime-message header close X so users dismiss messages through the visible action buttons. | Inspected the open `MOB.Request.New.Form` success message and confirmed K2 renders it as `.popup.dialog.runtime-message`; inspected the open `MOB.Request.Approval.Form` reject confirmation and confirmed K2 renders it as `.popup.dialog.confirmation-message`; both variants use `.message-box-content .panel` for the inner framed surface. Parsed the updated stylesheet with balanced blocks before publishing. | BahamasStyle must be updated to the new cache-busted CSS URL for final runtime verification. The inner-frame rule is scoped to K2 popup dialogs and leaves normal form panels and OK/Cancel message actions intact; close-X hiding remains scoped to `.runtime-message` dialogs only. | Ready for QA |
| GOB-K2-015 | 2026-07-31 | K2 read-only detail views | Normalized inline label/value pairs in read-only K2 detail cells so labels and values align cleanly in the Approval form Request Details view. | Inspected the open `MOB.Request.Approval.Form/?SerialNo=58293_28` page and confirmed K2 renders the affected cells as direct `.SourceCode-Forms-Controls-Web-Label + .SourceCode-Forms-Controls-Web-DataLabel` pairs with mismatched font size, line height, padding, and label margin. Parsed the updated stylesheet with balanced blocks before publishing. | The rule is structural and does not depend on generated cell names or Request Details control names. It applies only to K2 editor cells that contain a direct Label followed by a DataLabel, leaving standalone editable field labels unchanged. | Ready for QA |
| GOB-K2-016 | 2026-07-31 | K2 list view toolbars | Hid the empty toolbar/filter band on list views where K2 renders only hidden toolbar rows, removing the white gap above Task History column headers. | Inspected the open `MOB.Request.Approval.Form/?SerialNo=58293_45&ReqID=36` page and confirmed the Task History list renders a visible `.grid-toolbars` wrapper with a single `.toolbar.hidden` child while filters are disabled. Verified the structural selector matches only that hidden-only toolbar container on the page and parsed the updated stylesheet with balanced blocks. | The rule is generic to K2 grid toolbar structure and is not tied to Task History, ReqID, or generated view names. List views with visible filters, quick search, or toolbar actions keep their normal toolbar height. | Ready for QA |
| GOB-K2-017 | 2026-07-31 | K2 list view headers | Added the same gold section marker used by item-view panel headers to list-view grid headers so Task History and other list titles match the surrounding form views. | Inspected the open Approval form and confirmed Task History renders as `.grid > .grid-header` with no `::before` marker while neighboring panel headers render the existing gold marker. Parsed the updated stylesheet with balanced blocks before publishing. | The rule is generic to K2 grid headers and does not depend on Task History or generated view names. Empty grid headers that are already hidden remain hidden. | Ready for QA |
| GOB-K2-018 | 2026-08-03 | K2 Style Profile cleanup | Removed generated-cell compatibility selectors and manual grid row/column placement from the Bahamas style profile so Designer owns the rearranged form layout. | Verified the stylesheet contains no `[name="Cell Cell..."]` selectors, no old policy/action cell row placement rules, and no hard-coded primary-action offset. Parsed the updated stylesheet with balanced blocks and ran `git diff --check`. | Forms should use stable K2 runtime classes and authored `MOF_*` hooks only where visual styling is intentional. The cleanup keeps generic responsive header placement and component sizing rules, but removes the fragile layout overrides that were only needed before the form was rearranged. | Ready for QA |
| GOB-K2-019 | 2026-08-03 | K2 page background | Replaced the flat gray runtime canvas with a top-starting aqua-to-white page gradient that stretches across the full form document height. | Parsed the updated stylesheet with balanced blocks, ran `git diff --check`, and verified the flat `#e9eef1` body override was replaced by the same gradient stops used by the theme root. | The gradient is pure CSS and requires no image asset. It is applied at the page/body shell level so panels and controls remain white while the background grows with the form length. | Ready for QA |
| GOB-K2-020 | 2026-08-03 | K2 responsive form width | Set the desktop SmartForm shell to a 60vw maximum width while relaxing the width at tablet and mobile breakpoints so forms remain usable on smaller screens. | Parsed the updated stylesheet with balanced blocks and ran `git diff --check`. Verified the `runtime-form` width remains token-driven through `--gob-content-max-width`, with desktop, tablet, and mobile values defined in responsive CSS. | Desktop forms now center at 60% of the viewport. Below 1200px the form expands to the available viewport with gutters, and below 768px it uses the existing mobile padding with full available width. | Ready for QA |
| GOB-K2-021 | 2026-08-03 | K2 row control alignment | Vertically centered editor cells in multi-control rows by normalizing K2 `align-middle` cells and cells that directly contain standard input, choice, textarea, or button controls. | Parsed the updated stylesheet with balanced blocks and ran `git diff --check`. Verified the selector is structural against `.root-table > .editor-cell` and avoids generated cell names. | The rule preserves Designer-owned row placement and does not require control naming. Section headings and text-only description cells remain governed by their existing heading/text rules. | Ready for QA |
| GOB-K2-022 | 2026-08-03 | K2 typography | Switched the Bahamas profile font stack to `Inter, sans-serif`, loaded Inter from Google Fonts, and normalized K2 field labels to the shared 16px label/control size. | Parsed the updated stylesheet with balanced blocks and ran `git diff --check`. Verified K2 label selectors now use `--gob-label-font-size`, which is tied to the 16px control-size token. | The stylesheet imports Inter weights 400, 500, 600, and 700. If a runtime blocks Google Fonts, browsers fall back to their default sans-serif. Label-like controls now share the same size as standard input text for visual consistency. | Ready for QA |
| GOB-K2-023 | 2026-08-03 | K2 control typography | Reduced shared K2 control and label text from 16px to 12px by updating the common control-size token. | Parsed the updated stylesheet with balanced blocks and ran `git diff --check`. Verified controls and labels continue to inherit from `--gob-control-font-size` and `--gob-label-font-size`. | This affects standard inputs, dropdowns, picker text, choice text, read-only label/value pairs, and K2 labels that use the shared tokens. Headings, helper text, and list headers keep their separate hierarchy sizes. | Ready for QA |
| GOB-K2-024 | 2026-08-03 | K2 label/choice alignment | Vertically centered label cells that sit beside radio or checkbox choice-control cells in the same K2 row. | Parsed the updated stylesheet with balanced blocks and ran `git diff --check`. Verified the selector is structural against adjacent `.editor-cell` siblings and avoids field/control names. | This removes the label bottom margin only for label cells directly followed by a K2 choice-control cell, keeping normal stacked labels unchanged. | Ready for QA |
| GOB-K2-025 | 2026-08-04 | K2 read-only DataLabels | Removed the grey readonly fill from K2 display-only DataLabel values and normalized DataLabel typography to the same shared control scale used by field labels. | Inspected `MOBNewDevice-ApprovalsTABS.Form?REQUESTID=70` in Chrome and confirmed Device Details values render as `.SFC.SourceCode-Forms-Controls-Web-DataLabel`, with grey values carrying `.readonly`; added the narrow readonly background override plus shared DataLabel font sizing, then parsed the stylesheet with balanced blocks. | The override is generic to K2 DataLabel controls and does not depend on form, view, cell, or control names. Readonly form inputs continue to use the subtle grey readonly surface. | Ready for QA |
| GOB-K2-026 | 2026-08-04 | K2 reusable row headers | Added a reusable `MOD_ViewHeaderRow_*` hook so authored cells or controls can reuse the K2 view-header band styling, including the subtle surface, navy title text, borders, and gold marker. | Parsed `gob-k2-style-profile.css` with a Node balanced-brace check and ran `git diff --check`. | In K2 Designer, place a full-width Label/Data Label in the desired row and name it with the `MOD_ViewHeaderRow_` prefix, or apply `MOD_ViewHeaderRow` as a cell/control CSS class where available. Multi-cell row layouts should use a spanning cell for a full-width header band. | Ready for QA |
| GOB-K2-027 | 2026-08-04 | K2 view header alignment | Removed K2's extra left padding from panel header text so built-in view headers align with authored `MOF_SectionHeading` rows. | Inspected the live `MOBNewDevice-ApprovalsTABS.Form?REQUESTID=70` header DOM and confirmed `REQUEST DETAILS` had clean text but `.panel-header-text` added 10px of left padding; parsed `gob-k2-style-profile.css` with a Node balanced-brace check and ran `git diff --check`. | This is a visual alignment-only CSS change for K2 panel headers. Authored view titles and `MOF_SectionHeading` rows remain Designer-owned. | Ready for QA |
| GOB-K2-028 | 2026-08-04 | K2 authored section headings | Matched `MOF_SectionHeading` rows more closely to K2 view headers by using the same 52px header height, centered flex alignment, 15px title size, and left title inset. | Compared computed styles for the live `REQUEST DETAILS` panel header and the `DEVICE DETAILS` `MOF_SectionHeading` cell on `MOBNewDevice-ApprovalsTABS.Form?REQUESTID=70`; parsed `gob-k2-style-profile.css` with a Node balanced-brace check and ran `git diff --check`. | This keeps the existing `MOF_SectionHeading` selector and outside row spacing, but normalizes the header band itself. Designer still owns where section heading rows are placed. | Ready for QA |
| CTC-K2-001 | 2026-07-30 | Generic K2 Style Profile | Added a standalone `ctc-k2-style-profile.css` for neutral enterprise SmartForms styling across item views, list views, editable lists, standard controls, attachments, tabs, popups, validation, workflow action bars, dashboards, dark mode, responsive behavior, forced colors, reduced motion, and print. | Researched Nintex K2 Style Profile, view, and control documentation; consulted K2, enterprise design, QA/accessibility, and Claude design reviewers; verified balanced CSS structure, zero Bahamas/MOF/MOB/GOB branding selector hits, and AA contrast for representative light/dark/status token pairs. | Link the CSS last in Style Profile external files, disable Legacy Theme, and use a cache-busted URL during QA. Rich Text iframe internals, custom controls, barcode/image annotation, report/chart internals, and K2 popup markup that renders outside `.theme-entry` require live K2 runtime verification. | Ready for QA |
| CTC-K2-002 | 2026-07-30 | Generic K2 controls and list views | Refined the CTC profile with borderless dropdown and choice resting states, red filled destructive buttons with faded disabled styling, and expanded list/editable-list accommodations for toolbars, filters, paging, selected/current/new/dirty/deleted rows, inline controls, row validation, and loading states. | Parsed the stylesheet for balanced blocks; verified zero Bahamas/MOF/MOB/GOB branding hits in the CTC CSS; checked AA contrast for destructive button, destructive disabled, and representative list-row state token pairs. | Use a new cache-busted stylesheet URL in K2 Designer after deployment. Live K2 runtime QA should focus on dropdown composites, choice-list wrappers, destructive toolbar commands, editable-list save/delete flows, paging, filters, and row validation in both light and dark mode. | Ready for QA |
| CTC-K2-003 | 2026-07-30 | K2 calendar controls | Removed the K2 calendar control's inner wrapper divider while preserving the outer control surface and focus treatment. | Inspected the live `MOB.NewDevice.SPList` runtime page and confirmed calendar controls render as `.input-control.select-box.lookup-box.calendar-control.datepicker` with a `1px` border on `.input-control-m-c.styling-inner-wrapper`; parsed the updated stylesheet for balanced blocks. | Use a new cache-busted stylesheet URL in K2 Designer. QA should verify date inputs, calendar buttons, disabled/read-only calendars, and calendar popups in item views and editable-list cells. | Ready for QA |
| CTC-K2-014 | 2026-07-30 | K2 list filters and dark mode | Compacted list/editable-list filter rows and hardened the CTC theme for readable light and dark modes, including opt-in auto dark mode, explicit dark theme tokens, K2 wrapper color fixes, popup fallbacks, and dense filter-row composite controls. | Consulted CSS dark-mode and K2 list/editable-list reviewers; parsed the stylesheet for balanced blocks; verified representative light/dark/filter/destructive contrast pairs. | OS dark mode no longer activates automatically unless `.ctc-auto-dark` is present. Use `.ctc-theme-dark` or `.k2-theme-dark` for explicit dark mode, and test filter rows with text, dropdown, lookup, picker, and calendar filters in list and editable-list views. | Ready for QA |
| CTC-K2-017 | 2026-07-30 | K2 list toolbar filters | Compacted the real K2 list/editable-list toolbar filter container used by `MOB.NewDevice.SPList`, including selected-filter dropdowns, quick-search dropdowns, text inputs, nested K2 input wrappers, and inline filter/search buttons. | Inspected the open `MOB.NewDevice.SPList` runtime form and confirmed the top filter area renders as `.filterContainer` with `.filterChosen.filter`, `.quickSearch.filter`, `.searchDdl`, `.filterTxt`, nested `input-control-*` wrappers, and inline toolbar buttons; parsed the updated stylesheet for balanced blocks. | Use a new cache-busted stylesheet URL in K2 Designer. QA should verify selected filters, quick search, filter configuration, editable-list toolbars, and mobile wrapping in both light and explicit dark themes. | Ready for QA |
| CTC-K2-018 | 2026-07-30 | K2 list toolbar filter textboxes | Matched quick-search textbox rounding to list toolbar dropdown controls by rounding the visible `.filterTxt` shell and nested K2 textbox wrappers. | Compared computed styles on the open `MOB.NewDevice.SPList` filter toolbar and found dropdowns render with a `4px` radius while the textbox wrapper chain still exposed squarer inner edges; parsed the updated stylesheet for balanced blocks. | Use a new cache-busted stylesheet URL in K2 Designer. QA should verify quick-search text fields beside selected-filter and field dropdowns in list and editable-list toolbars. | Ready for QA |
| CTC-K2-021 | 2026-07-30 | K2 quick-search textbox radius | Added a more specific quick-search filter override for K2's `.quickSearch .filterTxt .input-control.text-input` wrapper chain so the visible textbox shell, inner cell, wrapper, input, and focus treatment round consistently with toolbar dropdowns. | Inspected the open Chrome `MOB.NewDevice.SPList` runtime page and found K2's runtime rule for `.filterContainer .quickSearch .filterTxt .input-control .input-control-m-c` still forced a squarer `2px` inner radius; parsed the updated stylesheet for balanced blocks. | Use a new cache-busted stylesheet URL in K2 Designer. QA should confirm the quick-search textbox corners match the adjacent field dropdown in normal, active/focused, and empty states. | Ready for QA |
| CTC-K2-019 | 2026-07-30 | K2 slider Data Label package | Added the final `k2-slider-*` Data Label bundle and implementation guide for opening an existing K2 item view in a right-side slider after a list row click, including backdrop lock, loader control, optional crest loading logo, close action, script bootstrap from the `slider js` Data Label, and reusable success/error/warning/info notifications. | Inspected the live `MOB.NewDevice.SPList` runtime DOM for the list and item view targets; verified the tagged CSS/JS wrappers; parsed all Data Label JavaScript snippets after stripping their `<script>` wrappers; verified no `{datetime}` or `RunId` placeholders remain in the snippets; documented the K2 rule order in `docs/k2-slider-smartforms.md`. | Use only the `k2-slider-*` files documented in `docs/k2-slider-smartforms.md`. Load `k2-slider-styles.datalabel.css` and `k2-slider-library.datalabel.js` on Form Initialized, set `k2-slider-on-list-row-click-open.datalabel.js` during the row-click rule, run K2 population actions, then set `k2-slider-after-view-populated-hide-loader.datalabel.js`; use `k2-slider-on-action-complete-close.datalabel.js` for close actions, `k2-slider-notify-helper.datalabel.js` for notifications, and `assets/bahamas-coat-of-arms.png` only if you want the included loading logo. | Ready for QA |
| CTC-K2-020 | 2026-07-30 | K2 slider execution-point naming | Renamed form-specific slider action snippets to generic execution-point names and replaced the default slider instance, target selector, title, loader text, and notification fallback with reusable detail-slider values. | Verified the renamed Data Label JS files keep valid `<script>` wrappers; parsed each snippet after stripping the wrapper; confirmed the current slider package, README, and slider guide no longer reference form-specific New Device names. | Replace any Designer references to the old action snippet files with `k2-slider-on-list-row-click-open.datalabel.js`, `k2-slider-after-view-populated-hide-loader.datalabel.js`, and `k2-slider-on-action-complete-close.datalabel.js`. Edit the open snippet target selector from `[name="Detail Item View"]` to the actual K2 detail view name for each form. | Ready for QA |

### QA Passed

| ID | Date | Area | Summary | Verification | Release Notes | Status |
| --- | --- | --- | --- | --- | --- | --- |

### Blocked

| ID | Date | Area | Summary | Blocker | Needed To Unblock | Status |
| --- | --- | --- | --- | --- | --- | --- |
